// Barrel for FakeIP shell + design primitives (мокапы fakeip-page-layout-v2 /
// fakeip-overview-fixed). Каркас и общие примитивы шапки.
export { default as FakeIPPageShell, type ShellChip } from './FakeIPPageShell.svelte';
export { default as FakeIPHero } from './FakeIPHero.svelte';
export { formatCompactCount } from './formatCount';
